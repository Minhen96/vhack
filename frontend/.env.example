# =============================================================================
# Frontend — Vite environment variables (example)
# Copy to frontend/.env and adjust if the sim server runs on a different port.
# Only VITE_* variables are exposed to the browser.
# =============================================================================

# Go Sim Server WebSocket URL — UI connects here for live drone + heatmap updates
VITE_WS_URL=ws://localhost:8080/ws/ui