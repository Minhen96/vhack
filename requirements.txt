# =============================================================================
# Python dependencies — covers backend + drone services
# Install with: pip install -r requirements.txt
# =============================================================================

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# MCP Python SDK
mcp>=1.0.0

# LangChain (Agent Framework)
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0

# LLM providers (uncomment the one(s) you use)
langchain-openai>=0.2.0           # OpenAI / DeepSeek (OpenAI-compatible API)
# langchain-google-genai>=2.0.0   # Gemini
# langchain-anthropic>=0.3.0      # Anthropic

# LangGraph (ReAct agent)
langgraph>=0.2.0

# HTTP client
httpx>=0.27.0

# WebSocket client (drone → sim server)
websockets>=12.0

# Data validation
pydantic>=2.0.0

# Env config
python-dotenv>=1.0.0
